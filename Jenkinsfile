pipeline {
  agent any
  triggers {
    pollSCM '* * * * *'
  }
  stages {
    stage('stage1') {
      steps {
        // One or more steps need to be included within the steps block.
      }

    }
    stage('stage2') {
      steps {
        // One or more steps need to be included within the steps block.
      }

    }
    stage('stage3') {
      steps {
        // One or more steps need to be included within the steps block.
      }

    }
    stage('stage4') {
      steps {
        // One or more steps need to be included within the steps block.
      }

    }
  }
}
